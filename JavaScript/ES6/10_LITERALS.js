const athlete = {
    name: "Jon David",
    sport: "Basketball",
    height: 180
}

const intro = `${athlete.name} is a ${athlete.sport} player.
He is ${athlete.height}cm tall.`;

console.log(intro);
